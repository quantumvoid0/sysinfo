{
  "commands": ["os","kernel","host","uptime","cpu","me","swap total","ram used","cores"],
  "colors": {
    "ascii": "white",
  }
}

// available colors as of now : red , green , yellow , blue , magenta , cyan , white
// any argument from `sys` is valid , you can check all valid arguments by typing in `sys` into your terminal
// NOTE : the `sys fetch` , `sys proc` & `sys live` arguments do not work with fetch so dont include it here as it will only break the application 
