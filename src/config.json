{
	"commands": ["os","kernel","host","uptime","cpu","me","swap total","ram used","language"],
	"colors": {
		"ascii": "white"
	},
	"icons": {
		"root": "",
		"ram used": ""
	}
}

// if syntax is wrong , it will revert to default config, check wiki for syntax
// available colors as of now : red , green , yellow , blue , magenta , cyan , white
// any argument from `sys` is valid , you can check all valid arguments by typing in `sys` into your terminal
// NOTE : the `sys fetch` , `sys proc`, `sys version-latest`, `sys update`, `sys drives`  & `sys live` arguments do not work with fetch.
